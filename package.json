{
	"name": "wide-erp-pos-root",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
		"build": "npm run build --prefix backend && npm run build --prefix frontend"
	},
	"devDependencies": {
		"@types/helmet": "^0.0.48",
		"concurrently": "^8.2.2"
	},
	"dependencies": {
		"helmet": "^8.1.0",
		"sharp": "^0.34.5"
	}
}
